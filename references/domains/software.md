# Software Engineering and APIs

Collect runtime, framework, entry points, public interfaces, stores, compatibility constraints, and nearest tests. Follow existing validation, error, logging, and dependency conventions.

Specify behavior and edge cases, validate trust boundaries, keep authorization separate from authentication, preserve consumed API and error contracts, and avoid unrelated refactors. Verify with proportionate formatting, static checks, focused tests, and builds.
