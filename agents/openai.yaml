interface:
  display_name: "IntentClause"
  short_description: "Compile and execute a context-aware expert prompt"
  default_prompt: "Use $intent-clause to improve and execute this request."

policy:
  allow_implicit_invocation: false
