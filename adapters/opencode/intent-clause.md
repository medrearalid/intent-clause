---
description: Explicitly run IntentClause on the supplied request
---

The user explicitly invoked IntentClause. Load the `intent-clause` skill with the skill tool, then treat the following text as its complete invocation arguments:

$ARGUMENTS

If no arguments were supplied, ask for one concise request. Do not optimize or execute the request before loading the skill.
